/* Javascript to be used on the create alert pages */

/* Pull Brand Name based on DIN field */
/*$(document).ready(function(){
  $('#DIN').change(function(){
    $.getJSON( "drug_export.json", { DRUG_IDENTIFICATION_NUMBER: } )
      .done(function( json ) {
        $('#BRAND_NAME').val(json.BRAND_NAME);
      })
      .fail(function( jqxhr, textStatus, error ) {
        $('#BRAND_NAME').val('');
    });
  }); 
});*/
